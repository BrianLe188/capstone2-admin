export enum EROLE {
  USER = "user",
  AMDIN = "admin",
}

export enum ELEVEL {
  UNIVERSITY = 'university',
  AFTER_UNIVERSITY = 'after_university',
}

export enum EFILE {
  docx = 'docx',
  pdf = 'pdf',
  txt = 'txt',
  xlsx = 'xlsx',
}